# LISTAS SÃO MUTÁVEIS!!!
lanche = ['pizza', 'pudim', 'suco', 'cookie', 'refrigerante']

# Ordenar Lista
lanche.sort()
print(f'Lista ordenada (alfabeticamente): {lanche}')
lanche.sort(reverse=True)
print(f'Lista ordenada (alfabeticamente de trás pra frente): {lanche}')

# Tamanho da Lista
print(f'Tamanho da lista \'lanche\': {len(lanche)}')


